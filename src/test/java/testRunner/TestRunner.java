package testRunner;

import io.cucumber.junit.platform.engine.Cucumber;

@Cucumber
public class TestRunner {

    // This class does not need additional methods.
    // The @Cucumber annotation automatically detects the feature files and step definitions.
}
